# 汇川 API 登录 e3 联调记录

## 结论

自动登录的错误码丢失、重复重试问题已在代码中修正。17:45按用户要求改用说明书示例密码进行有限测试，现场编辑级和管理级登录均成功，并通过实际权限回读。随后按用户“修改吧”的要求，生产源码的汇川默认API凭据已改为实测通过的值，保留数据库显式覆盖；不能据此将完整运动验收标记为通过。

此前使用用户提供的示教器密码时，现场返回 `e3`，该错误码本身仅表示参数值不合法。17:45的对照结果确认：同一协议、同一级别、同一许可前置条件下，现场API接受说明书示例密码、不接受此前提供的密码，问题已定位到API登录使用的密码不同。官方 SDK 与当前品牌底层发包格式一致，无须更换用户名或将密码改成MD5；不能将单台现场结果推广为所有汇川设备的密码规则。

## 已核对的证据

- 现场控制器地址为 `192.168.39.6:2222`，固件回读 `V4R25C2SPC0L27F100`。
- SDK 来源为用户提供的 `E:\WorkFile\汇川机器人\ReleaseToUser_2.zip`，内层 SDK DLL 版本为 `4.25.2.12`。
- 官方 DLL 在仅绑定 `127.0.0.1` 的模拟服务器上发送 `UserLogin <级别> <密码>`，级别为2，密码按用户已确认的六字节明文传输；请求/应答边界分别为 `@@/$$` 和 `##/$$`。模拟器成功不代表现场登录成功。
- PDF 和 SDK 随包 Word 手册均说明管理级为2，示例注释说明使用示教器密码；没有额外用户名参数。没有采用手册示例中的其他密码。
- 旧程序的被动抓包中，TCP连接确认成功，UserLogin返回e3。未保存包含凭据的抓包文件。

## 现场有限验证

旧程序退出后，执行一次许可前置验证。第一次前置只读检查使用了错误的任务查询名称，返回e1即终止，未获取许可、未发送登录。改用手册及生产驱动的 `Get_TaskRunSts 0` 后，执行以下流程：

| 操作 | 结果 |
| --- | --- |
| Get_ConnectState | 1 |
| CurCtrlDev | 2，远程以太网 |
| CurPermit | 0，无持有者 |
| Get_MotorSts | 0，伺服关闭 |
| Get_MotionSts | 0，无运动 |
| Get_TaskRunSts 0 | 0 |
| Get_DsMode | 0 |
| AcqPermit，无 forcibly | ok |
| CurPermit回读 | 1，本连接持有 |
| UserLogin，级别2，用户提供的密码 | e3 |
| RemovePermit | ok |
| CurPermit收尾回读 | 0，无持有者 |
| 伺服、运动收尾回读 | 均为0 |

未发送上电、运动、复位、切模式或程序执行指令，没有修改控制器密码。结果说明仅增加普通许可获取不能解决本次e3，因此没有把此诊断动作强行添加到自动连接路径。

## 代码修正

- `InovanceUserLogin.h` 只提取受限长度的错误码，不回显应答正文、命令参数和密码。对e3保留不确定性，不将它直接归因为密码错误。
- 认证失败或结果不确定后，锁存错误并暂停后台登录重试；人工连接动作允许一次新尝试。
- `InovanceRobotDriver.cpp` 保留失败证据，附加固件与权限只读诊断。未完成认证的连接不发布成功，业务指令仍拒绝执行。
- 普通TCP连接失败不受认证锁存影响，仍使用原后台重连逻辑。
- 默认凭据留在品牌底层，数据库显式配置覆盖方式不变。没有修改数据库或其他品牌登录流程。

## 17时41分前的待核查项

需要汇川确认现场固件 `V4R25C2SPC0L27F100` 对 `UserLogin` 的实际参数校验条件：为何同一管理员级别、示教器确认可用的密码在API上返回e3；是否存在对应的已知固件问题或不同的API登录密码设置。应提供现场固件对应的SDK、协议修订说明或明确配置入口。

更新：下文17:45的示例密码实测已验证API可成功登录。无需以更换协议或固件作为当前首选解决方式；若需解释API与示教器为何接受不同密码，仍应由厂家确认其配置关系。

未经用户授权，不轮询其他密码或厂家级别，不跳过权限回读，不尝试通过运动验证登录。

## 17时41分 用户授权的级别对照测试

2026-09-04 17:41:10（北京时间），用户要求切换几个级别测试。保持同一个已确认密码，按顺序对编辑级1、管理级2、厂家级3各执行一次登录，不试其他密码。

先回读确认模式1、伺服0、运动0、Task0状态0、数据流0、控制设备2、许可空闲、API用户级别0；普通获取本连接许可并回读成功后才提交登录。

| 请求级别 | UserLogin应答 | CurUserType实际级别 | UserLogout | 退出后级别 | 机器人状态 |
| --- | --- | --- | --- | --- | --- |
| 1 编辑 | e3 | 0 客户 | ok | 0 | 与测试前一致 |
| 2 管理 | e3 | 0 客户 | ok | 0 | 与测试前一致 |
| 3 厂家 | e3 | 0 客户 | ok | 0 | 与测试前一致 |

结束时 `RemovePermit=ok`、许可回读为0，无持有者；API用户级别为0。模式、伺服、运动、Task0和数据流状态均与测试前相同，没有上电、运动或切换运行模式。

结论：使用当前密码切换这三个已定义级别，均不能解决e3。不能仅凭此结果区分密码校验失败和固件参数约束，也不能认定任何登录已经成功。本次仅补充测试记录，未更改生产代码、数据库登录配置或Release程序。

## 17时45分 用户授权的说明书示例测试

2026-09-04 17:45:15（北京时间），用户明确要求使用说明书示例测试。核对PDF第143页字符串示例的编辑级1，以及第13页API示例的管理级2；仅使用说明书给出的同一个六字节示例密码，各测试一次。凭据值不写入此报告。

测试前确认机器人模式1、伺服0、运动0、Task0状态0、数据流0、控制设备2、许可空闲和API客户级0。普通获取许可并确认本连接持有后，再分别执行登录、权限回读、退出、退出后回读。

| 请求级别 | UserLogin应答 | CurUserType实际级别 | UserLogout | 退出后级别 | 机器人状态 |
| --- | --- | --- | --- | --- | --- |
| 1 编辑 | ok | 1 编辑 | ok | 0 客户 | 与测试前一致 |
| 2 管理 | ok | 2 管理 | ok | 0 客户 | 与测试前一致 |

结束时 `RemovePermit=ok`，许可回读为0，无持有者，API用户级别为0；模式、伺服、运动、Task0和数据流全部与测试前一致。没有执行同页的回原点、数据流开启或其他运动示例。

结论：现场API的编辑级、管理级登录已通过实测，差异在所用密码。本次只做用户要求的测试并补充记录，未更改生产源码的默认密码、数据库配置或Release。下一步应在用户确认后使用已实测通过的API凭据，并验证完整连接初始化；不改示教器密码、不放宽权限回读和运动安全门槛。

## 默认凭据修改与17时55分连接顺序验证

用户确认修改后，`include/InovanceUserLogin.h` 的品牌默认API密码已切换为实测通过的说明书示例值，默认用户级别仍为2。保留数据库非空显式密码覆盖，并在离线测试中加入默认值、默认发包和显式覆盖的独立断言；SDK离线回环探针同步采用新默认值。未修改机器人密码、FTP凭据或示教器密码。

2026-09-04 17:55:02（北京时间），只读查询现场 `Data/ConfigStore.db`，RobotC不存在 `ApiPassword` 覆盖项，因此正常启动会使用新品牌默认密码，无须改数据库。

另以新源码中的默认值进行一次连接顺序实测：确认机器人空闲、伺服关闭、许可空闲后，不获取控制许可，直接按生产连接路径提交管理级登录。结果 `UserLogin=ok`、`CurUserType=2`，许可仍为0，证明无需把许可获取移到自动登录之前。

随后 `UserLogout=ok`，用户级别恢复0、许可保持空闲；模式1、伺服0、运动0、Task0状态0、数据流0均保持不变。此次仅验证连接与登录，不代表新EXE的完整初始化、运动、扫描或焊接已通过现场验收。

本轮验证：登录默认值/覆盖/权限回读/脱敏/重试锁存测试通过；15组离线模式组合测试、汇川适配静态检查、业务适配层边界检查、验收流程检查、官方SDK回环发包检查均通过。

Release于2026-09-04 17:57:40更新到 `E:\WorkFile\bowen\QtWidgetsApplication4\x64\Release\QtWidgetsApplication4.exe`，编译0错误、8个既有SDK相关警告。EXE为9,261,568字节，文件版本2026.08.24.1333，SHA-256为 `6C23C883210C97111D398E39CD6174CC5F1006E70CE5F02206DDCB9F7F4A0A3A`。未自动启动界面、未提交或推送仓库。
