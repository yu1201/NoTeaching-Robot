# NoTeaching-Robot

免示教机器人上位机项目，基于 Qt + Visual Studio 开发，当前主要集成了：

- STEP 机器人驱动
- FANUC 机器人驱动
- FANUC KAREL/TP 文件生成、发送与调用
- 机器人点动、状态监控、功能测试
- 先测后焊流程
- 相机参数、手眼矩阵、精测量参数编辑

## 下载

- 仓库首页：<https://github.com/yu1201/NoTeaching-Robot>
- 最新安装包页面：<https://github.com/yu1201/NoTeaching-Robot/releases/latest>
- 当前源码版本：`v2026.05.08`

建议普通使用者直接从 `Releases` 页面下载安装包 `NoTeaching-Robot-Setup.exe`，不用自己编译源码。

## 2026-05-08 更新

- 版本号更新为 `v2026.05.08`，启动器版本和安装脚本默认版本已同步
- STEP/FANUC 基础能力进一步统一：连接、FTP、读取位置/关节、变量读写、MOVL/MOVJ、功能测试、点动目标运动和先测后焊执行入口都优先走通用驱动接口
- STEP 被动时间戳临时使用 PC steady 时间轴，FANUC 继续使用机器人端 `robot_ms`，先测后焊扫描匹配链路按统一时间轴处理
- 手眼自动标定主流程改为读取位置变量并调用通用运动接口，保留 FANUC 机器人侧检测/LS 上传作为专用功能
- 主界面机器人切换支持中文名 + 驱动类型显示，修复 GBK 配置中文名乱码；未知/不可用驱动会禁用现场按钮，管理页面仍可进入
- 大按钮主页默认最大化，普通子界面统一增加“返回主页”按钮，返回时保留未保存/运动中拦截逻辑
- 登录页继续收口为深色单卡片样式，支持账号记忆、保存密码、自动登录、游客登录和主页退出登录
- 当前剩余专用边界：FANUC 服务包上传、CURPOS/raw 诊断、LS 上传仍为 FANUC 专用；STEP 长按连续点动和机器人侧手眼验证后续再补

## 2026-05-07 更新

- 主界面重构为“大按钮快速操作 + 管理页面”结构，面向新手保留连接服务、先测后焊、坡口相机预览、读取当前位置和标定入口
- 新增本地账号登录、退出、注册和角色权限，权限分为操作员、工程师、管理员，首次启动自动创建默认管理员账号
- 高级入口增加工程师/管理员权限校验，注册账号仅允许管理员执行，账号数据保存在本地 `Data/Accounts.ini`
- 先测后焊焊道补偿继续修正下枪安全位方向、RZ 斜面旋转限制和稳定参考法向，补偿后轨迹恢复为约 `2mm` 间距
- 相机与机器人时间戳联调继续推进，被动监控时间戳接入扫描匹配链路，CLI 继续支持上传 FANUC 服务程序
- 相机帧缓存、UDP 拼包和 UI 日志做了进一步内存控制，长时间运行时更稳

## 2026-04-30 更新

- 先测后焊特征提取改为新拐点函数直通流程，原始点直接做特征识别，拐点之间按 2mm 扩充后生成焊接姿态和补偿文件，旧分类器不再参与直通流程
- 当前位置获取联调采用 `PR[3]/PR[4] + JOINT2POS` 方案，主动和被动读取都先读当前工具/用户坐标寄存器再反算直角位置，用于排查工具坐标与示教器显示差异
- 原因是 `CURPOS` 在 FANUC 侧只能直接拿到法兰/当前基准位置；如果只用它，就容易出现程序读到的位置和示教器显示的 TCP 位置不一致
- 机器人点动界面修复后台线程弹窗导致的 GUI thread 断言，T 轴步进和状态提示现在都回到界面线程处理
- Release 版本号更新为 `v2026.04.30`，安装包脚本默认版本和启动器版本号已同步

## 2026-04-28 更新

- 补偿参数界面重做为“姿态补偿 / 焊道补偿”切换模式，支持机器人选择、补偿类型下拉、参数重新读取与手动保存
- 焊道补偿新增“焊道方向补偿”，与 Z 向、枪反向补偿一起写入配置并接入先测后焊补偿后处理
- 姿态补偿界面新增当前姿态值显示，补偿编辑框统一右对齐，补偿类型下拉框改为波纹板等焊道类型
- 相机时间戳链路改为使用相机库时间戳，命令行支持扫描速度和相机时间补偿参数，当前默认时间补偿为 `-310 ms`
- 手动保存类参数界面增加关闭确认：未修改直接关闭，已修改时提示保存、不保存或取消关闭
- 统一下拉框样式与标题栏深色外观，修复部分界面下拉箭头显示异常

## 2026-04-27 更新

- 精测量参数新增“相机读取帧率（fps）”，扫描时按配置帧率读取相机缓存
- 扫描时间对齐改成“相机点等待机器人时间戳追上后再插值”，尾部超前点会短时等待，仍未匹配则跳过
- 机器人位姿采样继续保持独立节拍，避免只调相机读取频率时影响插补质量

## 2026-04-24 更新

- 打通先测后焊焊接执行链路：下枪/收枪安全位、多点 TP 下发、寄存器统一完成判断、关键节点确认弹窗
- 焊接轨迹新增 `CNT` 过渡，减少每点 `FINE` 带来的停顿
- 新增“跳过扫描焊接”，可直接读取历史姿态文件并按当前补偿参数重生成补偿后文件再执行
- 焊接结果文件拆分为“姿态文件”与“补偿后文件”两层输出
- Release 构建稳定配置调整为 `/Zm2000`，默认关闭 `/MP`

## 开发环境

当前工程按本机路径配置，直接编译前需要先准备以下依赖：

- Visual Studio 2022
- Qt 6.7.3 MSVC 2022 x64
- OpenCV 4.6.0
- Eigen 3.4.0
- Orocos KDL

工程里仍保留了 STEP/FANUC 相关接口、KAREL 源文件和示例 LS 文件，但运行期 DLL、`x64` 输出目录、日志和结果文件都已排除，不再上传到仓库。

## 目录说明

- `include`：头文件
- `src`：源码实现
- `Data`：机器人参数、工艺参数、相机参数
- `SDK/FANUC`：FANUC KAREL、LS、测试程序
- `SDK/STEP`：STEP SDK 头文件和链接库
- `Job`：示例任务/缓存任务文件
- `icons`：界面图标资源

## 文档

项目代码和 Notion 文档是绑定维护的。GitHub 里保留了一份便于直接预览的 Markdown 版本，Notion 继续作为协作主文档。

- Notion 总入口：<https://www.notion.so/336c868d819b80a181c7ef8a393b53ad>
- GitHub 文档索引：[`docs/README.md`](docs/README.md)
- 工作流程：[`docs/workflow.md`](docs/workflow.md)
- 界面说明：[`docs/ui.md`](docs/ui.md)
- 波纹板项目方案：[`docs/wavy-board-plan.md`](docs/wavy-board-plan.md)
- 问题表摘要：[`docs/issues.md`](docs/issues.md)
- 工作记录摘要：[`docs/worklog.md`](docs/worklog.md)

## 当前功能

### STEP / FANUC 驱动

- 根据 `RobotType` 自动切换机器人驱动
- STEP 常规控制接口
- FANUC TCP 常驻服务通信
- FANUC LS/KL/PC 文件发送与调用
- MOVJ / MOVL / 连续点动 / 固定 TP 调用

### UI 界面

- 主界面统一深色风格
- 功能测试子界面
- 机器人点动控制子界面
- 工艺参数编辑界面
- 精测量数据修改界面
- 补偿参数编辑界面
- 手眼矩阵参数界面
- 先测后焊流程界面

### 流程与数据

- 扫描安全位采用脉冲点
- 扫描起点/终点采用直角坐标
- 扫描过程中采集相机点、机器人位姿、激光点
- 激光点计算支持时间插值
- 支持按机器人和相机维度独立保存配置

## 当前待排查问题

- 手眼矩阵计算结果存在偏差，待检查是否为矩阵本身问题
- 获取 `PR[80]` 点位结果仍有异常，待继续联调检测

## 构建说明

1. 用 Visual Studio 2022 打开 `QtWidgetsApplication4.sln`
2. 确认本机 Qt / OpenCV / Eigen / Orocos KDL 路径与工程配置一致
3. 编译 `Debug|x64` 或 `Release|x64`

说明：

- `QtWidgetsApplication4.vcxproj` 当前仍带有本机绝对路径配置，如果换电脑，需要先改 Qt / OpenCV / Eigen / KDL 路径
- STEP 的 `Robot-SDKd.lib` / `Robot-SDK.lib` 需要自行放到 `SDK/STEP`

## 仓库精简说明

这次已经把下面这些内容从仓库管理中排除了：

- Visual Studio 中间文件和编译输出
- 运行日志、结果文件、临时目录
- 本地备份参数文件
- STEP / KAREL 手册和压缩包
- 重复的 SDK 副本目录

如果需要保留本地文档或备份文件，可以继续放在工程目录里，但不会再进入 Git。
